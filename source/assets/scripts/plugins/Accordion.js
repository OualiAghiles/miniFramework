class Accordion {

	constructor(className) {
    this.els = document.querySelector(className)
    this.accordions = Array.from(this.else)
		this.accordions.forEach(accordion => {
      this.toggle(accordion)
    })
  }
  
  this.show () {

  }

  this.hide () {

  }

  this.toggle(accordion) {
    const headers = accordion.querySelectorAll('header')
    headers.forEach(h => {
      h.addEventListener('click', (e) {
        
      })
    })

  }
}